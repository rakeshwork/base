<?php if (! defined ( 'BASEPATH' ))	exit ( 'No direct script access allowed' );

/**
 * Move to AUTHETICATION HELPER LATER
 *
 * @param unknown_type $bRedirect
 * @param unknown_type $sRedirectTo
 * @return unknown
 */
/*
function isAdminLoggedIn($bRedirect=false, $sRedirectTo='home'){
	
	$CI = &get_instance();
	return $CI->authentication->is_admin_logged_in($bRedirect, $sRedirectTo);
}
*/

/**
 * Move to AUTHETICATION HELPER LATER
 *
 * @param unknown_type $bRedirect
 * @param unknown_type $sRedirectTo
 * @return unknown
 */
/*
function isUserLoggedIn($bRedirect=false, $sRedirectTo='home', $bReturnObject=false){
	
	$CI = &get_instance();
	return $CI->authentication->is_user_logged_in($bRedirect, $sRedirectTo, $bReturnObject);
}
*/